## Novedades v0.1.8

- Correccion en buscador de ventas para codigos exactos (`SKU`/codigo de barras), incluyendo casos numericos como `124` y `125`.
- Al presionar `Enter`, si el codigo coincide exacto, se agrega directamente la prenda correcta.
- Se mantiene la logica de relevancia lograda anteriormente para busquedas por texto (sin degradar resultados por nombre).
